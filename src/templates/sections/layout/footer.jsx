import styles from '@/styles/sections/layout/footer.module.scss'

export default function Footer(){
    return (
        <footer className={styles.footer}>
            <h1 className={styles.title}></h1>
        </footer>
    )
}